export const pairingStatuses = ['pending','claimed','active','expired','rejected','closed'];
export const transferStatuses = ['uploading','ready','downloaded','expired','failed','deleted'];
export const MAX_FILE_SIZE = 100 * 1024 * 1024;
